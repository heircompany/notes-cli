const titleOptions = {
  describe: `title of note`,
  demand: true,
  alias: `t`
}

const bodyOptions = {
  describe: `body of note`,
  demand: true,
  alias: `b`
}

module.exports = {
  titleOptions,
  bodyOptions
}
